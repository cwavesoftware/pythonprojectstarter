from setuptools import find_packages, setup
setup(
    name='scriptstarter',
    packages=find_packages(),
    version='0.1.0',
    description='Creates a script with logging, argparse and other utilities ready',
    author='Cosmin Craciun (cWave)',
    license='MIT',
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    test_suite='',
)